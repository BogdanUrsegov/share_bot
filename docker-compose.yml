version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8003:8000"
    volumes:
      - ./bot:/app/bot
      - ./data:/data
    restart: unless-stopped
    command: python -m bot.main

volumes:
  sqlite_data: